{
  "name": "beachside-comms",
  "version": "1.0.0",
  "description": "Beachside EP — Communications Platform",
  "scripts": {
    "queue": "node queue-builder.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
